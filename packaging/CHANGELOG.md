## 1.0.1

- Fixed rare edge case for dead clients that died far away from the campfire where the map wouldn't load for them if the host were to continue from saved progress.
- Fixed mod not being disabled for clients when the host doesn't have it installed.

## 1.0.0

Initial release.
