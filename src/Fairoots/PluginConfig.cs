using BepInEx.Configuration;

namespace Fairoots
{
    /// <summary>
    /// Config plumbing scaffold. No settings bound yet - the preset system
    /// (seed field, preset 1-4 selector, per-mechanic overrides) lands with
    /// the first real implementation phase. See ROADMAP.md.
    /// </summary>
    public class PluginConfig
    {
        public PluginConfig(ConfigFile config)
        {
        }
    }
}
